name: Update Comment

on:
  workflow_dispatch:
jobs:
  update_comment:
    runs-on: ubuntu-latest
    steps:
    - name: Update Comment
      uses: peter-evans/create-or-update-comment@v2
      with:
        comment-id: 1398843948
        body: |
         Update body test
